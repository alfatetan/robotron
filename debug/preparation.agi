#!/usr/bin/python3

from agidebug import AgiDebug
import sys

log = AgiDebug('/var/lib/asterisk/agi-bin/protocol_preparation.txt')
log.upd('Попробуем записать через класс с использованием ' \
        'импорта.')
for _ in range(21):
    line = sys.stdin.readline()
    log.upd(line)
